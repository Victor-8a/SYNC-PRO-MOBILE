import 'package:flutter/material.dart';

class PaginaVendedores  extends StatelessWidget {
  const PaginaVendedores({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Página de Busquedas'),
    );
  }
}
