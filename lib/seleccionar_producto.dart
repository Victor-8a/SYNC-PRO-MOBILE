import 'package:flutter/material.dart';
class seleccionar_producto extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Aquí puedes implementar la lógica para mostrar una lista de productos
    return Scaffold(
      appBar: AppBar(
        title: Text('Seleccionar Producto'),
      ),
      body: Center(
        child: Text('Pantalla de Selección de Productos'),
      ),
    );
  }
}