
import 'package:flutter/material.dart' show AppBar, BuildContext, Center, Scaffold, StatelessWidget, Text, Widget;

// Esta es tu nueva página, puedes personalizarla según tus necesidades
// ignore: camel_case_types
class second_page extends StatelessWidget {
  const second_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Inicio Super Sistemas'),
      ),
      body: const Center( 
      ),
      
    );
  }
}
