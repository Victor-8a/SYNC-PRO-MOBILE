import 'package:flutter/material.dart';

class PaginaCliente extends StatelessWidget {
  const PaginaCliente({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Página de Cliente'),
    );
  }
}
